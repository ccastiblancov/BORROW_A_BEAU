<h1>Book your next partner</h1>
<%= form_with model: [@profile, @booking], url: profile_bookings_path(@profile) do |form| %>
  <div class="actions">
    <%= form.submit "Create Booking", class: "btn btn-primary" %>
  </div>
<% end %>
